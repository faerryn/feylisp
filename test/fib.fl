(let mem (list 1 1))
(let fib (func (n)
	       (if (< n (len mem))
		 (at mem n)
		 (push mem (let result (+ (fib (+ n -1)) (fib (+ n -2))))))))
(let i 0)
(while (<= i 100)
       (print "fib(" i ")\t= " (fib i) "\n")
       (let i (+ i 1)))
